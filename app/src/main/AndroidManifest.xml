<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reminderapp">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/background"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApp">
        <activity android:name="com.example.reminderapp.splashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.reminderapp.ReminderActivity" />
        <activity android:name="com.example.reminderapp.MainActivity">

        </activity>

        <receiver android:name="com.example.reminderapp.AlarmBrodcast" />
    </application>

</manifest>